# 硬件说明

本目录是可选文档入口。

只有硬件解释会帮助固件开发时才写，例如上电时序、启动脚配置、USB 拓扑、存储器连接、连接器引脚、板级限制等。

必须随仓库保存的硬件文件应放在根目录 `hardware/` 下：

```text
hardware/schematic/     原理图 PDF 或源文件，推荐保存
hardware/pcb/           PCB 源文件或导出资料，可选
hardware/production/    Gerber、BOM、坐标文件、生产说明，可选
```
