# Firmware Notes

本目录是固件实现说明入口。

- `legacy-firmware-features.md`：旧固件行为、兼容语义、用户可见规则。
- `new-firmware-dev-notes.md`：当前固件实现逻辑、模块职责、调用链和验证记录。
- `app-changelog.md`：APP 固件版本记录。

Boot/OTA 不是基础模板默认能力；需要时参考 `examples/boot-ota/`。
